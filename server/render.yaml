services:
  - type: web
    name: poker-club-server
    env: node
    plan: free
    buildCommand: npm install
    startCommand: node src/index.js
    autoDeploy: true
    healthCheckPath: /health
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 8080
